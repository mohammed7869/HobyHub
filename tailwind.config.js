/** @type {import('tailwindcss').Config} */
module.exports = {
    theme: {
      extend: {
        colors: {
          navbar: "#1E3A8A", // Same as bg-blue-900
          primary: "#1E3A8A", // Button color
        },
      },
    },
    plugins: [],
  };
  